spring:
  application:
    name: career-os
  datasource:
    url: jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:6543/postgres?user=postgres.mgqmcfawkgiwjdhxazmy&password=${DB_PASSWORD}&sslmode=require
    username: postgres.mgqmcfawkgiwjdhxazmy
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
supabase:
  jwt:
    secret: ${JWT_SECRET}
    expiration: 86400000